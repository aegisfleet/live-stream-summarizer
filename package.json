{
  "name": "live-stream-summarizer",
  "version": "1.0.0",
  "description": "ホロライブ配信アーカイブ要約サイト",
  "scripts": {
    "fetch-schedule": "node scripts/fetch-schedule",
    "check-archive": "node scripts/check-archive",
    "summarize": "node scripts/summarize",
    "cleanup-summaries": "node scripts/cleanup-summaries",
    "generate-pages": "node scripts/generate-pages",
    "start": "node server.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.11.0",
    "cheerio": "^1.0.0-rc.12",
    "compression": "^1.8.1",
    "dotenv": "^16.3.1",
    "express": "^5.1.0",
    "googleapis": "^128.0.0"
  },
  "devDependencies": {
    "cypress": "^14.5.4"
  }
}
